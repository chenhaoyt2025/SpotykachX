# SpotykachX 项目说明

## 项目目的

`SpotykachX` 基于 `Spotykach_clean_165_ring` 新建。

这个项目从 clean 基线开始建立双 deck 引擎框架；当前硬件映射已经恢复为官方 Spotykach 映射，后续用于：

1. 每个 deck 不只可以作为录音带引擎，也可以切换为其他声音引擎。
2. 每个 deck 现有的效果部分，后续可以作为效果器路径来使用。
3. 后续可以继续增加新的效果类型和新的声音引擎类型。

## 当前阶段范围

本阶段只做框架，不主动改现有声音行为：

1. 新建独立项目目录 `SpotykachX`，与原 `Spotykach_clean_165_ring` 分开。
2. 记录 clean 基线相对官方原版已经做过的修改。
3. 在代码里加入每个 deck 的引擎类型框架。
4. 默认引擎仍是原 Spotykach tape engine，硬件输入和 LED ring 使用官方映射。

本阶段不做：

1. 不修改库代码。
2. 不改 clean 基线项目本身。
3. 不把未确认的功能写死成正式映射。

## clean 基线在官方原版基础上已经修改的内容

这里说的 clean 基线，指的是：

`/Users/hoho_mini/Documents/Daisy Seed/Synthux Academy/Spotykach_clean_165_ring`

它相对官方 `v1.2.0` 基线，目前确认有这些业务代码改动：

1. `src/ui/core.ui.cpp`
   HC165 两个芯片读取顺序互换，用来适配你现在 DIY 硬件里 A/B 两组接反的问题。

2. `src/ui/led.ring.cpp`
   LED ring 的索引顺序调整，用来适配你现在 DIY 硬件里圆环方向与原版相反的问题。

3. `src/hw/hardware.cpp`
   `V/OCT A` 和 `MIX A` 的 CV 映射互换；
   `V/OCT B` 和 `MIX B` 的 CV 映射互换。
   这是为了适配当前 DIY 板上对应 CV 接线互换的问题。

4. `src/core/driver.h`
   `tap_tempo()` 返回 tap 后的 BPM，并在非外部同步时直接写回内部 tempo。

5. `src/core/tempo.cpp`
   tempo 平均值初值设为 `500.f`，用于避免初始 tempo 状态不稳定。

6. `src/ui/core.midi.cpp`
   USB MIDI 初始化和处理保持开启。

以上内容是在引入 `SpotykachX` 双 deck 引擎框架之前，clean 基线已经存在的修改，后续都要明确区分，不和新的引擎改造混在一起。

## 当前原版硬件映射状态

当前硬件已经恢复为与原版 Spotykach 相同，因此 `SpotykachX` 已移除以下旧 DIY 硬件补偿：

1. HC165 两个芯片的读取顺序互换已取消，恢复为 `sr1 = register 0`、`sr2 = register 1`。
2. LED ring 的反向索引补偿已取消，恢复官方 LED ring 映射。
3. `V/OCT A` 与 `MIX A`、`V/OCT B` 与 `MIX B` 的 CV 通道互换已取消，恢复官方 ADC 映射。

以下项目不属于硬件映射补偿，本次保持不变：

1. tap/tempo 相关修正。
2. USB/TRS MIDI 功能。
3. SpotykachX 新增引擎、`fx_only` 和 Deck B `through`。
4. SDMMC 已恢复原版 `MEDIUM_SLOW` 设置，正常传输时钟约 12.5 MHz。

## 本次新加的框架内容

本次在 `SpotykachX` 里新增的是最小引擎框架：

1. 新增 `src/core/engine.h`
2. 为每个 deck 增加独立 `EngineType`
3. 当前引擎顺序已经扩展为：
   `Tape`
   `AudreyII`
   `Oscillator`
   `Reverb`
   `Microcosm`
   `Plaits`
   `Rings`
   `Elements`
   `Benjolin`
4. `Noise` 槽位已被 `Microcosm` 取代，并新增 `Benjolin`

当前默认值全部仍是 `Tape`，所以不会主动改变现有声音路径。

补充：

1. 当前已经支持保存每个 deck 的 `EngineType` 和 `fx_only` 标记到独立 QSPI 设置区。
2. 当前已经接入隐藏切换动作：
   `Spot + Trig A/B` 循环切换对应 deck 的引擎类型，
   `Alt + Spot + Rev A/B` 切换对应 deck 的 `fx_only` 标记。
3. `Alt + Spot + SeqB` 用来切换 `Deck B` 的 `through` 直通模式。
4. 当前 9 个位置已经明确为：
   `Tape`
   `AudreyII`
   `Oscillator`
   `Reverb`
   `Microcosm`
   `Plaits`
   `Rings`
   `Elements`
   `Benjolin`
5. 其中 `AudreyII` 来自 `official/audrey-ii-HOHO`；
   `Microcosm` 为自实现粒子/循环引擎；
   `Plaits / Rings / Elements` 分别使用现成的 DaisySP 模块接入；
   `Benjolin` 为自实现的 rungler 双振荡器引擎。

## AudreyII 接入来源与边界

这次 `AudreyII` 只允许从下面这个项目作为来源：

`/Users/hoho_mini/Documents/Daisy Seed/Synthux Academy/official/audrey-ii-HOHO`

本次没有从其他本地未完成项目借代码。

同时坚持下面这几个边界：

1. 不修改任何库代码。
2. 只改 `SpotykachX` 项目业务代码。
3. 先让 `AudreyII` 以可编译、可切换的单 deck 引擎形式接入。
4. `Microcosm` 走自实现路线，不照搬吉他效果器逻辑。

## 当前 AudreyII 接入状态

目前状态：

1. `AudreyII / Oscillator / Reverb / Microcosm / Plaits / Rings / Elements / Benjolin` 都已经作为 `EngineType` 接入 `SpotykachX`。
2. 每个 deck 都可以独立切换引擎。
3. 原本 `Tape` 路径仍保留，默认仍然是 `Tape`。
4. 非 `Tape` 引擎输出后，仍然会继续走当前 deck 自己已有的 FX 路径。
5. `fx_only` 打开时，当前设计是跳过引擎本身，只把输入送进 deck FX。
6. `Deck B` 可以开启 `through`，把 `A` 的输出直通进 `B`。

## 已确认但容易混淆的事实

### 1. `undo` 不是 SpotykachX 新加功能

当前代码里看到的第三块主录音缓冲，也就是 `src/hw/buffer.sdram.cpp` 里的：

1. `_srcBuf1`
2. `_srcBuf2`
3. `_srcBuf3`（注释写为 `Undo`）

这部分在 `Spotykach_clean_165_ring` 里就已经存在。

所以结论是：

1. `undo buffer` 不是这次 `SpotykachX` 新加的。
2. 它至少在 `SpotykachX` 的 clean 基线阶段就已经存在。
3. 后续做内存预算时，必须把这块当成“现有基础内存占用”，不能误记成 `SpotykachX` 新增功能。

### 2. 当前真正占内存最大的部分

按当前代码结构，内存大头优先级大致是：

1. `Tape` 主录音 / undo 缓冲
2. 每个 deck 的 `Flux` 长延迟缓冲
3. `SpotykachX` 为 AudreyII 增加的 SDRAM pool

也就是说：

1. 现在最吃内存的不是 `Grit`
2. 也不只是新加引擎
3. 而是原本 `Tape` 录音体系本身

## 这次接入中修掉的问题

为了把 AudreyII 接进当前 `SpotykachX`，这次额外修了两类工程问题：

1. `AudreyII` 自带的 `BiquadFilters` 与 `SpotykachX` 现有 `core/biquad` 重复；
   现在已经改为直接复用 `SpotykachX` 现有 `biquad`，不再保留重复实现。
2. 新加的 `DeckEngine` 如果直接作为 `Core` 成员放在主 SRAM，会导致链接阶段 SRAM 爆掉；
   现在已经改为通过 Audrey 的 SDRAM 分配器把 deck engine 实例放到 SDRAM，主 SRAM 已恢复正常。

## 当前编译状态

当前 `SpotykachX` 已经重新编译通过。

输出文件：

1. `build/spotykachx.elf`
2. `build/spotykachx.hex`
3. `build/spotykachx.bin`

## 控制映射文档

详细控制、颜色、备用通道保留定义，单独整理在：

`控制映射.md`

## 当前已新增、相对 clean 基线不同的 SpotykachX 功能

这一节只记录 `SpotykachX` 相对 `Spotykach_clean_165_ring` 新增的内容。

目前确认有：

1. 新增 `EngineType` 框架：
   `Tape / AudreyII / Oscillator / Reverb / Microcosm / Plaits / Rings / Elements / Benjolin`
2. 新增每个 deck 独立的 engine 状态保存
3. 新增每个 deck 的 `fx_only` 状态保存
4. 新增隐藏操作：
   - `Spot + Trig A/B` 切换 engine
   - `Alt + Spot + Rev A/B` 切换 `fx_only`
5. 新增 `Alt + Spot + SeqB` 切换 `Deck B through`
6. 新增 `AudreyII / Oscillator / Reverb / Microcosm / Plaits / Rings / Elements / Benjolin`

当前还没有正式实现，但已经明确讨论过、后续可能进入实现阶段的内容：

1. 双 FX slot 可选效果架构
2. 固定单边 Tape 的瘦身版本
3. 多固件启动选择方案

`Deck B through` 和 `A -> B` 双 deck 串联路由已经实现，不再属于待实现功能。

## 后续实施方向

后续可以按这个顺序推进：

1. 先接入 deck 级 engine selection 的状态保存与读取。
2. 再决定 UI 切换动作。
3. 然后给单个 deck 接入第一个替代引擎。
4. 最后再扩展效果器模式与更多 effect type。

## 你当前提出的两个方向

这里只记录为方案，不代表已经决定。

### 方向一：固定一边 Tape，另一边切换引擎/效果

特点：

1. 一边保留完整 `Tape` 录音/回放能力
2. 另一边承担新声音引擎或主效果引擎
3. 更容易控制内存预算
4. 更容易维持 Spotykach 原本工作流

优点：

1. 风险低
2. 结构清楚
3. 更容易先做成稳定可用版本

缺点：

1. 灵活度低于双边都可切换
2. 玩法会被固定在“一边录音，一边处理/发声”的框架里

### 方向二：多固件整合，通过开机按键选择不同固件

特点：

1. 不同固件可有不同内存分配和功能重点
2. 例如：
   - 固件 A：原版 Spotykach 录音优先
   - 固件 B：大混响 / 大延迟优先
   - 固件 C：其他特化引擎优先

优点：

1. 每个固件可以针对自己的目标单独分配 SDRAM
2. 不必把所有能力硬塞进同一个固件
3. 更容易做出真正“重型效果器版”

缺点：

1. 维护成本更高
2. 设置、文档、烧录管理都更复杂
3. 开机选择逻辑和固件组织方式需要额外设计

### 当前判断

这两个方向都可以。

如果优先目标是尽快做出稳定版本：

1. 更推荐先走方向一

如果优先目标是最大化功能边界、按用途分专用版本：

1. 方向二更合适

## 这次修改的原则

1. 只在 `SpotykachX` 新项目里改。
2. 不回写旧项目。
3. 不修改任何库代码。
4. 先搭结构，默认行为先不变。
