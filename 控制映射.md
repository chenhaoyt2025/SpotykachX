烧录 

cd "/Users/hoho_mini/Documents/Daisy Seed/Synthux Academy/SpotykachX"
make program-dfu

# SpotykachX 控制映射

这份文档只记录当前 `SpotykachX` 代码里已经存在的实际映射。

原则：

1. 不把后续可能会做的功能提前写成既成事实。
2. 备用 4051 / 165 通道只登记，不分配功能。
3. `SpotykachX` 现在有两层“模式”：
   - 引擎模式：`Tape / AudreyII / Oscillator / Reverb / Microcosm / Plaits / Rings / Elements / Benjolin`
   - 原 Spotykach deck 模式：`Reel / Slice / Drift`

## 一、引擎模式切换

每个 deck 独立切换。

当前隐藏操作：

1. `Spot + Trig A`（代码中的 `SeqA`）
   - 循环切换 Deck A 的引擎模式
2. `Spot + Trig B`（代码中的 `SeqB`）
   - 循环切换 Deck B 的引擎模式
3. `Alt + Spot + Rev A` 或 `Alt + Spot + Rev B`
   - 切换对应 deck 的 `fx_only`
4. `Alt + Spot + SeqB`
   - 切换 `Deck B` 的直通 `through`

### Deck B `through` 信号路径

1. `through = off`：Deck B 使用原本分配给 B 的外部输入信号
2. `through = on`：Deck A 的完整输出改为 Deck B 的输入，即 `A engine/Tape -> A FX -> B engine/Tape -> B FX`
3. 如果 Deck B 是 Tape，A 的输出也可以作为 B 的录音输入
4. 最终仍保留 A 输出和 B 输出两路，由 Crossfader 混合
5. Crossfader 在 A 端时主要听 A；在 B 端时主要听经过 B 再处理的串联结果；中间位置同时混合两路
6. `through` 只支持 `A -> B`，没有 `B -> A`
7. `through = on` 时，Deck B ring 第 30 号点显示蓝色

当前 9 个引擎顺序：

1. `Tape`
2. `AudreyII`
3. `Oscillator`
4. `Reverb`
5. `Microcosm`
6. `Plaits`
7. `Rings`
8. `Elements`
9. `Benjolin`

## 二、颜色定义

### 1. 原 Spotykach deck 模式颜色

1. `Reel`：橙色 `#F7941D`
2. `Slice`：蓝色 `#0064FF`
3. `Drift`：紫色 `#C850FF`

### 2. 时钟来源颜色

1. `internal`：绿色 `#00FF00`
2. `ts4 / 外部时钟`：粉色 `#FF00FF`
3. `midi`：青色 `#00FFEF`

### 3. 效果颜色

1. `Flux`：红粉色 `#FF6565`
2. `Grit soft`：黄 `#FFD524`
3. `Grit harsh`：橙 `#FF9A24`

### 4. 引擎模式颜色

这是通过 `Spot + Trig A/B` 切换引擎后，环形 LED 用来显示当前 engine 的颜色。

1. `Tape`：橙色 `#F7941D`
2. `AudreyII`：绿色 `#00FF00`
3. `Oscillator`：黄色 `#FFDE21`
4. `Reverb`：蓝色 `#0000FF`
5. `Microcosm`：粉色 `#FF00FF`
6. `Plaits`：青色 `#00B4D8`
7. `Rings`：白色 `#FFFFFF`
8. `Elements`：紫色 `#C850FF`
9. `Benjolin`：黄绿色 `#B6FF00`

### 5. 引擎模式环形 LED 显示规则

1. `Tape`：亮 1 个点
2. `AudreyII`：亮 2 个点
3. `Oscillator`：亮 3 个点
4. `Reverb`：亮 4 个点
5. `Microcosm`：亮 5 个点
6. `Plaits`：亮 6 个点
7. `Rings`：亮 7 个点
8. `Elements`：亮 8 个点
9. `Benjolin`：亮 9 个点
10. 如果 `fx_only = on`，额外在 ring 的第 31 号点亮红色提示
11. 如果 `through = on`，额外在 ring 的第 30 号点亮蓝色提示

## 三、原始 Tape 引擎控制

只要当前 deck 的 engine 还是 `Tape`，它就按原 Spotykach 控制逻辑工作。

### 1. 旋钮基础定义

每个 deck 一组 7 个主旋钮：

1. `SOS`
2. `MOD FREQ`
3. `MOD AMT`
4. `SIZE`
5. `PITCH`
6. `POS`
7. `ENV`

全局还有：

1. `CROSSFADE`

### 2. Reel / Slice / Drift 下的主旋钮

#### `SOS`

1. 正常：`I/O Mix`
2. 按住 `Alt`：`Feedback`
3. 按住 `Flux` 触摸：`Flux Mix`
4. 按住 `Grit` 触摸：`Grit Mix`
5. 只有 Deck A：
   - `Tap` 正在按住时，`SOS A` 改的是全局 `Key Interval`

#### `SIZE`

1. `Reel`：`Size`
2. `Slice`：
   - 正常：`Size`
   - `Tap` 正在按住且当前 deck 非空：改 `Slice 长度 / 节拍拟合`
3. `Drift`：
   - 正常：`Size`
   - 按住 `Alt`：`Window Size`

#### `POS`

1. 正常：`Start Position`
2. 当前 deck 有 cue 时：
   - Deck A：`Alt + POS` 改 `Start Offset Interval`
   - Deck B：按当前代码实现，`POS` 仍会继续改 `Start Position`，同时 `Alt + POS` 也会改 `Start Offset Interval`
3. 按住 `Flux` 触摸：`Flux Feedback`

#### `ENV`

1. `Reel / Slice`：`Envelope Shape`
2. `Drift`：
   - 正常：`Envelope Shape`
   - 按住 `Alt`：`Envelope Size`

#### `PITCH`

1. 正常：`Speed / Pitch`
2. 按住 `Alt` 调节时：进入量化 pitch 调节
3. 按住 `Flux` 触摸：`Flux Intensity`
4. 按住 `Grit` 触摸：`Grit Intensity`
5. 当该 deck 处于存储槽选择状态时：
   - `PITCH` 改的是存储槽编号，不再改声音

#### `MOD FREQ`

1. 正常：当前 deck 的 `Mod Speed`
2. Deck A 在 `Tap` 按住时：改全局 `Tempo`
3. Deck B 在 `Tap` 按住时：改全局 `Pan Speed`

#### `MOD AMT`

1. 正常：当前 deck 的 `Mod Amount`
2. Deck A 在 `Tap` 按住时：改全局 `Click Mix`
3. Deck B 在 `Tap` 按住时：改全局 `Pan Range`

#### `CROSSFADE`

1. 始终控制全局 `Crossfade`

### 3. 触摸按键

每个 deck：

1. `Play`
   - 正常：播放
2. `Rev`
   - 正常：反向播放
3. `Alt + Play`
   - 外部录音
4. `Alt + Rev`
   - 内部录音
5. `Flux`
   - 正常按住：临时打开 Flux
   - `Alt + Flux`：切换 Flux Lock
6. `Grit`
   - 正常按住：临时打开 Grit
   - `Alt + Grit`：切换 Grit Lock
7. `Seq`
   - 正常：触发
   - `Alt + Seq`：序列 arm / disarm
   - 长按后继续按住：清空 sequence

全局：

1. `Alt`
   - 长按 `Tap` 再触摸 `Alt`：切换 clock source
2. `Spot`
   - 当前主要用于校准流程里的采集动作

### 4. 钮子开关 / 165 读取到的功能

#### Deck A 所在 165

1. `sr1 bit2 / bit3`
   - 全局路由三档：
   - `GenerativeStereo / DoubleMono / Stereo`
2. `sr1 bit1`
   - Mod A 类型：
   - `Follow` 或 `LFO`
3. `sr1 bit0`
   - 当 Mod A 为 LFO 时：
   - 切换 `square / random`
4. `sr1 bit6 / bit7`
   - Deck A 模式：
   - `Drift / Reel / Slice`
5. `sr1 bit4 / bit5`
   - Deck A 的 `Start Mod` 与 `Size Mod` 开关组合

#### Deck B 所在 165

1. `sr2 bit5`
   - Mod B 类型：
   - `Follow` 或 `LFO`
2. `sr2 bit4`
   - 当 Mod B 为 LFO 时：
   - 切换 `saw / sine`
3. `sr2 bit2 / bit3`
   - Deck B 模式：
   - `Drift / Reel / Slice`
4. `sr2 bit0 / bit1`
   - Deck B 的 `Start Mod` 与 `Size Mod` 开关组合
5. `sr2 bit6`
   - 手动 `Tap`

## 四、SpotykachX 新增 9 个 engine 的参数映射

下面这一节只在 engine 不是 `Tape` 时生效。

注意：

1. 这些映射是当前 `deck.engine.cpp` 里的实际实现。
2. `Oscillator / Reverb / Microcosm / Plaits / Rings / Elements / Benjolin` 都是实际引擎。
3. 不代表它们已经是最终面板设计。

### 1. `Tape`

1. 仍按上一节原 Spotykach 逻辑工作

### 2. `AudreyII`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：混响混合量
3. `MOD FREQ`：反馈高通截止
4. `MOD AMT`：混响反馈
5. `SIZE`：反馈延迟时间
6. `PITCH`：String Pitch
7. `POS`：Echo Delay Time
8. `ENV`：反馈低通截止
9. `Feedback`：反馈增益
10. `Flux Mix`：Echo Send
11. `Flux Intensity`：Echo Feedback

当前未使用：

1. `Grit Mix`
2. `Grit Intensity`
3. `mode`
4. `reverse`

### 3. `Oscillator`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：振荡器/滤波混合
3. `SIZE`：双振荡器 detune
4. `PITCH`：基频
5. `POS`：输入驱动的 FM 深度
6. `ENV`：滤波器截止
7. `Feedback`：滤波器共振
8. `Flux Mix`：外部输入混入量

当前未使用：

1. `MOD AMT`
2. `Grit Mix`
3. `Grit Intensity`
4. `mode`
5. `reverse`

### 4. `Reverb`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：干湿比
3. `MOD FREQ`：LFO 频率
4. `MOD AMT`：LFO 深度
5. `ENV`：音色滤波截止
6. `Feedback`：反馈量

当前未使用：

1. `SIZE`
2. `PITCH`
3. `POS`
4. `Flux Mix`
5. `Flux Intensity`
6. `Grit Mix`
7. `Grit Intensity`
8. `mode`
9. `reverse`

### 5. `Microcosm`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：干湿比
3. `MOD FREQ`：粒子/切片切换速度
4. `MOD AMT`：播放速率偏移
5. `SIZE`：粒段长度
6. `PITCH`：循环位置中心
7. `POS`：延迟/采样窗口中心
8. `ENV`：反馈/驱动
9. `Flux Mix`：输入混入量
10. `Flux Intensity`：粒段抖动
11. `Grit Mix`：驱动量
12. `Grit Intensity`：降采样强度
13. `Feedback`：回授量

当前未使用：

1. `mode`
2. `reverse`

### 6. `Plaits`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：干湿比
3. `MOD FREQ`：共振频率
4. `MOD AMT`：Bleed
5. `SIZE`：Shape
6. `PITCH`：Carrier frequency
7. `POS`：输出声像偏移
8. `Flux Mix`：输入混入量

当前未使用：

1. `ENV`
2. `Flux Intensity`
3. `Grit Mix`
4. `Grit Intensity`
5. `Feedback`
6. `mode`
7. `reverse`

### 7. `Rings`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：干湿比
3. `SIZE`：结构
4. `PITCH`：频率
5. `POS`：立体声偏移
6. `ENV`：亮度
7. `Flux Mix`：输入激励
8. `Flux Intensity`：额外激励强度
9. `Feedback`：阻尼

当前未使用：

1. `MOD FREQ`
2. `MOD AMT`
3. `Grit Mix`
4. `Grit Intensity`
5. `mode`
6. `reverse`

### 8. `Elements`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：干湿比
3. `MOD FREQ`：共振激励频率
4. `MOD AMT`：Accent
5. `SIZE`：结构
6. `PITCH`：频率
7. `POS`：立体声偏移
8. `ENV`：亮度
9. `Flux Mix`：持续激励开关
10. `Flux Intensity`：ZOscillator 波形偏移
11. `Grit Mix`：额外驱动
12. `Grit Intensity`：ZOscillator mode
13. `Feedback`：阻尼

当前未使用：

1. `mode`
2. `reverse`

### 9. `Benjolin`

当前参数映射：

1. `SOS`：输出电平
2. `MIX`：滤波输出混合方式
3. `MOD FREQ`：Osc B / Rungler clock 频率
4. `MOD AMT`：Rungler 回灌到 Osc A 的深度
5. `SIZE`：Osc B 相对频率偏移
6. `PITCH`：Osc A 主频率
7. `POS`：Rungler CV 注入滤波器的深度
8. `ENV`：滤波器截止
9. `Flux Mix`：XOR 波形混入量
10. `Flux Intensity`：Rungler 方波混入量
11. `Grit Mix`：外部输入混入量
12. `Grit Intensity`：Rungler 直接调制输出量
13. `Feedback`：滤波器共振

当前未使用：

1. `mode`
2. `reverse`

## 五、MIDI 控制

本节按照当前 `SpotykachX` 代码记录。USB MIDI 与 TRS MIDI 使用同一套消息处理和控制映射。

### 1. MIDI 接口

1. `TRS MIDI`：UART MIDI，`D14 = RX`，`D13 = TX`
2. `USB MIDI`：使用前置/外部 USB 接口，工作于 USB MIDI Device 模式，不是 USB Host
3. 两个接口可以同时接收 MIDI
4. MIDI Clock 同时从 TRS MIDI Out 和 USB MIDI 发出

### 2. 默认 MIDI 通道

1. `Deck A`：MIDI Channel 1
2. `Deck B`：MIDI Channel 2
3. 其他通道不用于 deck 控制

可以通过 SD 卡中的 `SK/config.txt` 修改：

```text
mid_ch_a
1
mid_ch_b
2
mid_ps_a
0
mid_ps_b
0
```

其中：

1. `mid_ch_a / mid_ch_b`：设置 Deck A/B 的 MIDI 通道，填写 `1..16`
2. `mid_ps_a / mid_ps_b`：是否让 MIDI Start/Continue/Stop 控制对应 deck，`0 = 关闭`，`1 = 开启`
3. 没有读取到配置文件时，使用默认 Channel 1/2，MIDI Start/Stop 联动默认关闭

### 3. MIDI Note

1. 只处理 `Note On`
2. Channel 1 默认触发 Deck A，Channel 2 默认触发 Deck B
3. `Note 60` 对应原速 `1x`
4. 音符编号相对 60 每变化一个半音，触发播放速度按半音比例变化
5. Velocity 当前不参与运算
6. `Note Off` 当前不处理
7. 当前 Note On 进入原 Spotykach deck 的触发路径，不是为新增声音引擎单独实现的音符触发接口

### 4. MIDI Clock / Start / Stop

1. 当 clock source 切换到 `midi` 时，接收到的 `Timing Clock` 控制系统速度
2. `Start` 和 `Continue` 会复位内部时钟位置
3. 当对应的 `mid_ps_a / mid_ps_b = 1` 时，`Start / Continue` 会播放非空 deck
4. 当对应的 `mid_ps_a / mid_ps_b = 1` 时，`Stop` 会停止对应 deck
5. MIDI Clock 来源的 LED 颜色为青色 `#00FFEF`

### 5. MIDI CC 映射

普通连续参数把 MIDI 值 `0..127` 映射为内部值 `0.0..1.0`。

| CC | 功能 | 作用范围 / 说明 |
|---:|---|---|
| 3 | Crossfade | 全局 A/B Crossfade；A 或 B 的有效通道都可控制 |
| 14 | External Record | 外部录音；值大于 0 时触发一次 |
| 15 | Internal Record | 内部录音；值大于 0 时触发一次 |
| 20 | Start Position | 对应 deck 的起始位置 |
| 21 | Start Offset | 对应 deck 的起始偏移间隔 |
| 22 | Size | 对应 deck 的 Size |
| 23 | Envelope | 对应 deck 的 Envelope Shape |
| 24 | Pitch | 对应 deck 的 Speed / Pitch |
| 25 | I/O Mix | 对应 deck 的 I/O Mix；新增 engine 也从这个共享参数取得 `SOS / MIX` |
| 26 | Deck Feedback | 对应 deck 的 Feedback |
| 27 | Envelope Size | 对应 deck 的 Envelope Size |
| 28 | Window Size | 对应 deck 的 Window Size |
| 85 | Forward Play | 正向播放切换；值大于 0 时触发一次 |
| 86 | Reverse Play | 反向播放切换；值大于 0 时触发一次 |
| 89 | Mod Cycle | 对应 deck 的 Mod Speed / Cycle |
| 90 | Mod Glow | 对应 deck 的 Mod Amount / Glow |
| 102 | Grit On | `0 = off`，`1..127 = on` |
| 103 | Grit Intensity | 对应 deck 的 Grit Intensity |
| 104 | Grit Mix | 对应 deck 的 Grit Mix |
| 105 | Flux On | `0 = off`，`1..127 = on` |
| 106 | Flux Intensity | 对应 deck 的 Flux Intensity |
| 107 | Flux Feedback | 对应 deck 的 Flux Feedback |
| 108 | Flux Mix | 对应 deck 的 Flux Mix |

对于非 Tape engine，CC 仍修改同名的共享参数；该参数在不同 engine 中的实际含义，按本文“新增 9 个 engine 的参数映射”执行。例如 CC 24 在新增 engine 中会控制该 engine 的 `PITCH` 参数。

### 6. MIDI Out 当前内容

1. TRS MIDI Out 和 USB MIDI Out 当前只发送 `System Realtime Clock`
2. 当前不发送 Note、CC、Start、Continue 或 Stop

### 7. 当前没有实现的 MIDI 控制

1. `Note Off`
2. Velocity
3. Pitch Bend
4. Aftertouch
5. Program Change
6. MIDI 切换 engine 类型
7. MIDI 切换 `fx_only`
8. MIDI 切换 Deck B `through`

## 六、备用通道保留定义

这里只登记，不赋予功能。

### 1. 4051 备用通道

按当前 `hardware.cpp`：

1. `Mux1 / 第一个 4051 / kMux1SignalPin = A9`
2. 已使用通道：`0, 1, 2, 3, 4, 5, 6`
3. 当前唯一未使用通道：`channel 7`

建议保留名：

1. `CTRL_SPARE_MUX1_CH7`

### 2. 165 备用通道

按当前 `_process_switches()`：

1. `sr1` 的 `bit0..bit7` 已全部使用
2. `sr2` 的 `bit0..bit6` 已使用
3. `sr2 bit7` 当前未使用

建议保留名：

1. `SW_SPARE_SR2_BIT7`

## 七、当前明确不要动的保留项

以下通道现在只做占位记录，后续是否使用必须单独确认：

1. `4051 channel 7 on mux1`
2. `165 sr2 bit7`

当前文档目的就是先把它们冻结住，避免后面在没有确认的情况下被我擅自占用。
